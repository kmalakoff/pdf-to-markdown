# Changelog

All notable output-affecting changes to this project are documented here.
`OUTPUT_VERSION` (see `src/report.ts`) bumps whenever a change can alter output for identical
input — geometry, emission, engine configuration, tuning defaults, review/float marking.

## OUTPUT_VERSION 1 — initial

Initial release.
